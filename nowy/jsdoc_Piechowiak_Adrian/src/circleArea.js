/**
 * calculates area
 * @param{number} r - promien
 * @returns{number} - wynik
 * @throws {Error} if 'r' is '<0'
 * @author Adrian Piechowiak 5D
**/



function calculaetArea(r) {
    if (r > 0) {
        return Math.PI * r * r;
    }
    else{
        return "dumas";
    }

    const wynik
}